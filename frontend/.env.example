# Local API (leave unset on Vercel when using serverless /api proxy + BACKEND_URL)
VITE_BACKEND_URL=http://localhost:8000

# Production: WebSocket URL when REST goes through Vercel proxy (https://…ngrok-free.dev)
# VITE_WS_URL=https://xxxx.ngrok-free.dev
